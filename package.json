{
  "name": "claude-code-notify",
  "version": "0.1.0",
  "description": "Claude Code Stop hook desktop notification",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test hooks/notify.test.mjs"
  }
}